This project runs a Hono API with a PostgreSQL database on a local Kubernetes cluster using Minikube.
Kustomize is used to manage Kubernetes manifests, and the API is built as a Docker image and pulled from Docker Hub.
